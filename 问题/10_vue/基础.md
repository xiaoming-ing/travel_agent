### 01 什么是闭包？
函数“记住了”它诞生时的环境，
- 函数嵌套：函数内部定义了另一个函数
- 内部函数引用了外部函数定义的变量
- 返回了内部函数
```
function createCounter() {
    let count = 0

    return function() {
        count++
        console.log(count)
    }
}
const counter = createCounter()

counter() // 1
counter()//2 
```