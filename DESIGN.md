# Travel Agent Frontend Design Direction

## 1. 页面要表达什么

这个产品不是“AI 炫技页”，而是一个能帮助用户把旅行从想法落到可执行日程的规划工作台。

页面要表达三件事：

- 可信：推荐来自景点、天气、酒店、地图和用户偏好的综合判断，不是随口生成。
- 清晰：用户能快速看懂目的地、日期、每日安排、预算、酒店和交通关系。
- 可调整：行程不是一次性答案，而是可以继续对话、替换、微调、确认的方案。

整体气质应接近“城市旅行编辑部 + 专业行程调度台”，而不是“AI 聊天机器人落地页”。

## 2. 目标读者是谁

主要用户：

- 有明确目的地和日期，但不想自己反复查攻略的人。
- 想要一个可执行行程，而不是泛泛推荐清单的人。
- 对住宿、预算、交通、天气、距离有实际约束的普通旅行者。

次要用户：

- 想快速比较不同方案的自由行用户。
- 需要给家人、情侣、朋友整理行程的人。
- 对 AI 工具有兴趣，但更关心结果是否靠谱的人。

用户不是来欣赏“科技感”的。他们打开页面时的核心问题是：这趟旅行怎么安排更顺、哪里值得去、每天会不会太累、预算是否合理、还能不能改。

## 3. 主要参考是什么

视觉和信息组织参考：

- Apple Maps / Google Maps 的地点信息密度：克制、清晰、以地图和地点为中心。
- Airbnb Trips / Airbnb Experiences 的旅行内容表达：图片真实、留白干净、信息有生活感。
- Notion Calendar / Linear 的工作台体验：低噪音、高可读、状态明确。
- Monocle Travel Guide / Kinfolk Travel 的城市编辑气质：像一份被整理过的城市笔记，而不是营销广告。
- 高德/携程/飞猪的实用信息结构：日期、距离、价格、评分、营业信息这类字段要好找。

不要照搬单一产品。这个项目应融合“旅行内容的温度”和“规划工具的秩序”。

## 4. 参考里要学什么

从地图类产品学习：

- 地点、距离、路线、天气、酒店应该围绕真实决策排序。
- 地图和地点列表要形成互相解释的关系，不能只是装饰。
- 当前状态、选中项、路线顺序要明确。

从 Airbnb 学习：

- 图片要有真实目的地感，优先展示地点本身。
- 文案要具体，不要空泛夸赞。
- 卡片可以轻，但内容必须有层级。

从 Linear / Notion Calendar 学习：

- 页面要像工具一样稳定，控件位置可预期。
- 边框、分割线、状态标签、表格密度要有秩序。
- 交互反馈要轻，不需要夸张动画。

从旅行指南学习：

- 标题可以有编辑感，但不要过度文艺。
- 版式可以使用“行程单、地图标注、城市档案、日程索引”的视觉语言。
- 让页面像一份经过筛选的 itinerary，而不是模型输出的聊天记录。

## 5. 参考里不要学什么

不要学地图产品的广告和信息拥挤。这个项目不是 OTA 大卖场，不需要满屏价格、优惠、推荐位。

不要学 Airbnb 的大面积营销图片首屏。用户已经进入工具，不需要被再次销售。

不要学 Notion 的过度灰白和无品牌感。页面需要有旅行项目自己的识别度。

不要学传统旅游网站的景区宣传口吻。避免“必去”“不容错过”“完美无忧”“说走就走”等泛化表达。

不要学 AI SaaS 的模板套路：深色渐变、霓虹光效、玻璃拟态、漂浮卡片、机器人头像、紫蓝渐变按钮。

## 6. 颜色

颜色方向：城市地图、纸质行程单、交通标识、天气光线。

主色不要再用紫蓝渐变。新的色彩系统应偏克制、耐看、实用。

建议色板：

- `#F7F4EE` Sand Paper：页面底色，接近纸张但不要发黄。
- `#1F2933` Ink：主文字，稳定、清楚。
- `#5B6770` Slate Note：次级文字和说明。
- `#C44E2D` Signal Red：主行动按钮、当前步骤、重要提醒。
- `#1F6F78` Route Teal：地图路线、选中地点、链接。
- `#D8C7A3` Ticket Tan：预算、票价、辅助标签。
- `#E7E2D8` Border Warm：分割线、卡片边框。
- `#FFFFFF` Surface：表单、侧栏、结果模块。

使用原则：

- 大面积使用 Sand Paper / White / Ink，保证阅读稳定。
- Signal Red 只用于主要操作和关键状态，不能铺满页面。
- Route Teal 用于路线、地图、导航选中态，建立“行程路径”的联想。
- 不使用整页渐变背景。
- 不使用高饱和紫色、蓝紫渐变、霓虹绿、赛博黑。

## 7. 字体

字体方向：中文要清晰、安静、适合长时间阅读；数字和标签要有工具感。

推荐：

- 中文正文：`"PingFang SC"`, `"Microsoft YaHei"`, system-ui, sans-serif。
- 英文/数字/日期：`"Inter"`, `"SF Pro Text"`, system-ui, sans-serif。
- 地图编号、预算、日期标签：可以使用等宽数字特性，优先 `font-variant-numeric: tabular-nums`。

字号建议：

- 页面主标题：28-36px，字重 650-750。
- 分区标题：18-22px，字重 650。
- 卡片标题：15-17px，字重 600。
- 正文：14-15px，行高 1.65。
- 元信息、标签、时间、距离：12-13px，行高 1.4。

注意：

- 不要用过大的 hero 标题压倒工具本身。
- 不要用负字距。
- 不要在中文界面里混用太多花哨衬线字体。
- emoji 不作为标题图标使用。需要图标时用统一 icon 系统。

## 8. 首屏布局

首屏目标：让用户马上知道这是一个能开始规划、能查看历史、能继续修改的旅行工作台。

推荐桌面布局：

```text
┌─────────────────────────────────────────────────────────────┐
│ left rail       │ main planning surface                     │
│ user/history    │ ┌ destination + dates + preferences ┐     │
│ recent trips    │ │ compact trip intake form           │     │
│ knowledge base  │ └────────────────────────────────────┘     │
│                 │ ┌ live route preview / city notes     ┐     │
│                 │ │ map texture / recent plan summary   │     │
│                 │ └────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

首屏不要做营销型 hero。用户进入后应该直接看到可操作表单、历史行程和规划状态。

表单页：

- 保留目的地、日期、交通、住宿、偏好、额外要求。
- 信息密度提高，减少大白卡堆叠。
- 可把“目的地 + 日期”做成一个强输入区，其余偏好作为紧凑设置面板。

聊天页：

- 左侧对话应像“规划日志”，不是普通客服聊天。
- 右侧预览应突出当前方案，尽早展示目的地、天数、预算、路线顺序。
- 生成中状态用步骤列表或进度日志表达，不用“AI 正在施法”式文案。

结果页：

- 结果页是核心体验，应像一份专业 itinerary。
- 左侧目录固定，右侧内容按“总览 -> 地图 -> 每日安排 -> 酒店 -> 天气 -> 预算”组织。
- 每日安排要有清晰时间线和地点顺序，避免一堆同质卡片。

移动端：

- 左侧历史栏收起为顶部入口。
- 表单单列，结果页目录变成横向 sticky tabs。
- 地图、每日行程、酒店卡片必须保持可读，不依赖 hover。

## 9. 按钮和卡片

按钮：

- 主按钮使用实色 `Signal Red`，高度 40-44px，圆角 8px。
- 次按钮使用白底、Warm Border、Ink 文本。
- 危险/退出操作不要和主按钮同色，使用低饱和红或文字按钮。
- 按钮文案用动作结果命名：`开始规划`、`继续调整`、`查看完整行程`、`保存偏好`。
- 不要使用 `→`、emoji 或“立即体验 AI”这类营销文案作为主要识别。

卡片：

- 圆角控制在 8px，不使用 16px 以上的大圆角。
- 阴影要非常轻，优先使用边框和层级间距。
- 不要“卡片套卡片”。结果页的大模块可以是 section，重复项才用 card。
- 卡片头不要再用彩色渐变条。
- 卡片内容要有信息层级：标题、元信息、描述、决策字段、操作。

状态标签：

- 交通、住宿、预算、天气、距离使用小标签。
- 标签应像地图图例，不像促销 badge。
- 颜色数量控制在 3-4 种，避免彩虹标签。

## 10. 图片和素材

图片原则：

- 图片必须服务目的地和决策，不做纯装饰。
- 优先使用景点真实图片、地图截图/地图瓦片、高德 POI 图片或可追溯来源图片。
- 没有图片时，用结构化占位：地点编号、类型、地址、建议时长，而不是渐变占位图。

素材方向：

- 地图路线线条、地点编号、票据纹理、天气小图标、行程时间轴。
- 可以使用细线图标，但必须统一风格。
- 不要使用 3D 插画、机器人、星球、漂浮窗口、抽象渐变背景。

图片处理：

- 景点图使用 4:3 或 16:10 固定比例。
- 不使用过度暗角、模糊遮罩、玻璃叠层。
- 图片旁边必须有可读信息，不能只做视觉展示。

## 11. 动效

动效原则：帮助用户理解状态变化，不制造“AI 感”。

可以有：

- 表单 focus 的轻微边框颜色变化。
- 侧栏当前会话切换的 120-160ms 背景过渡。
- 结果页目录定位的 smooth scroll。
- 每日行程展开/收起的高度和透明度过渡。
- 生成进度逐条出现，像日志刷新。
- 地图 marker hover 或选中时轻微放大。

不要有：

- 大面积页面入场动画。
- 持续漂浮的装饰元素。
- 背景光斑、粒子、流光边框。
- 卡片大幅 hover 上浮。
- 每个模块都 scroll reveal。

必须支持：

- `prefers-reduced-motion: reduce` 时关闭非必要动画。
- 动效时长控制在 120-220ms。

## 12. 不要出现的 AI 味

明确禁止：

- 紫蓝渐变背景、渐变按钮、渐变卡片头。
- “AI 旅行助手”“智能规划”“完美无忧”“一键生成专属方案”等泛化标题。
- emoji 作为系统性视觉语言。
- 机器人头像、星球、脑电波、魔法棒、闪光、粒子。
- 玻璃拟态、毛玻璃浮层、霓虹边框。
- 大 hero + 三个卖点卡片 + CTA 的 SaaS 落地页结构。
- 没有真实内容支撑的统计数字。
- 每张卡片都带图标、阴影、大圆角。
- 文案里频繁出现“AI 正在”“智能推荐”“为你量身打造”。
- 空泛夸赞景点，例如“风景优美，值得一去”。

替代表达：

- 用“规划日志”替代“AI 思考中”。
- 用“正在查询天气 / 正在整理酒店 / 正在生成每日路线”替代“智能生成中”。
- 用“继续调整”替代“让 AI 优化”。
- 用“路线顺序”“预计停留”“交通建议”“预算影响”替代“推荐理由”。

最终判断标准：

如果去掉页面上的产品名，用户仍能看出这是一个旅行规划工具，而不是任意 AI SaaS 模板，这版设计才算合格。
